<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.camera.view.PreviewView
        android:id="@+id/cameraxmk_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <FrameLayout
        android:id="@+id/cameraxmk_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:elevation="3dp"
        android:orientation="horizontal"
        android:padding="32dp"
        android:visibility="gone"
        tools:targetApi="lollipop">

        <com.google.android.material.slider.Slider
            android:id="@+id/cameraxmk_slider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="16dp"
            android:background="@drawable/cameraxmk_slider"
            android:paddingHorizontal="16dp"
            android:paddingVertical="32dp"
            android:progressBackgroundTint="@android:color/white"
            app:haloColor="@color/blue_normal"
            app:thumbColor="@color/blue_normal" />
    </FrameLayout>
</FrameLayout>