<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mozhimen.uicorektest">

    <application
        android:name=".UicoreKApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwiftKit">

        <!-- <meta-data -->
        <!-- android:name="design_width_in_dp" -->
        <!-- android:value="800"/> -->
        <!-- <meta-data -->
        <!-- android:name="design_height_in_dp" -->
        <!-- android:value="1280"/> -->
        <activity
            android:name=".UicoreKActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- ///////////////////////////////////////////////////////////////////////////////////// -->
        <!-- adaptk -->
        <activity
            android:name=".adaptk.AdaptKActivity"
            android:exported="false" />
        <activity
            android:name=".adaptk.AdaptKSystemBarActivity"
            android:exported="false" />
    </application>

</manifest>