<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mozhimen.abilityktest">

    <!--trans-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:name=".AbilityKApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwiftKit">

        <activity
            android:name=".AbilityKActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- abilityk -->
        <!--scank-->
        <activity
            android:name=".scank.ScanKActivity"
            android:exported="false" />
        <activity
            android:name=".scank.ScanKHSVActivity"
            android:exported="false" />
        <activity
            android:name=".scank.ScanKFaceActivity"
            android:exported="false" />

        <!--opencvk-->
        <activity
            android:name=".opencvk.OpenCVKActivity"
            android:exported="false" />
        <activity
            android:name=".opencvk.OpenCVKMatchActivity"
            android:exported="false" />
        <activity
            android:name=".opencvk.OpenCVKContrastActivity"
            android:exported="false" />
        <activity
            android:name=".opencvk.OpenCVKShapeActivity"
            android:exported="false" />

        <!--transk-->
        <activity
            android:name=".transk.TransKActivity"
            android:exported="false" />

    </application>

</manifest>